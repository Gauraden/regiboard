PACKET_NAME='cgdb'
PACKET_VERSION='0.1'
PACKET_TARBALL="cgdb.tar.bz2"
PACKET_DESCRIPTION='tests for sys stats functions'

PACKET_CONFIGURE=""

PacketInstall() {
	local root_dir=$1
	local usr_bin_dir="$root_dir/usr/bin"
	mkdir -p $usr_bin_dir
	cp -P ${build_dir}/$PACKET_NAME $usr_bin_dir
}
